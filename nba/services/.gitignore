# Per-service Gradle build artifacts — never commit (regenerated by every build/test run).
*/.gradle/
*/build/

# staged by nba-model/run.ps1 from databricks/ml/rl_qnet.json (do not commit)
nba-model/rl_qnet.json
